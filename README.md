# Playroom Rental System

**Engineered** a Java client–server desktop application to **standardize** playroom rental business operations. This system **integrates** booking management, staff scheduling, and real-time data synchronization.

## Features
- **Architected** with a multi-module approach (Client, Server, Common).
- **Implemented** MVC design pattern to **ensure** separation of concerns.
- **Developed** socket-based communication to **handle** multiple concurrent users.
- **Integrated** MySQL database for **systematized** data persistence.

## Tech Stack
- **Languages:** Java
- **Architecture:** Model-View-Controller (MVC)
- **Networking:** TCP Sockets, Serialization
- **Database:** MySQL

## System Architecture
The client application **communicates** with the server via TCP sockets. The server **processes** incoming requests, **executes** database transactions, and **returns** standardized responses through a custom Common library.

